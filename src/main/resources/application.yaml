server:
  port: 8080

spring:
  application:
    name: mapper

  datasource:
    url: jdbc:postgresql://localhost:5435/mapper_db
    username: user
    password: pass
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
